import { Update } from './Update';
import { Api } from 'telegram';
import { Snake } from '../client';
export declare class UpdateMessageID extends Update {
    id: number;
    randomId: bigint;
    constructor();
    init(update: Api.UpdateMessageID, SnakeClient: Snake): Promise<this>;
}
