import { Api } from 'telegram';
import { From } from '../Utils/From';
import { Update } from './Update';
import { Snake } from '../client';
import { Media } from '../Utils/Media';
export declare class UpdateBotInlineQuery extends Update {
    id: bigint;
    from: From;
    query: string;
    location?: Media;
    chatType?: string;
    offset: string;
    constructor();
    init(update: Api.UpdateBotInlineQuery, SnakeClient: Snake): Promise<this>;
}
