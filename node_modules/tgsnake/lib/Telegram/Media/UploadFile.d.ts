/// <reference types="node" />
import { Api } from 'telegram';
import { Snake } from '../../client';
export interface uploadFileMoreParams {
    fileName?: string;
    workers?: number;
    onProgress?: onProgress;
}
interface onProgress {
    (progress: number): void;
    isCanceled?: boolean;
}
export declare function UploadFile(snakeClient: Snake, file: string | Buffer, more?: uploadFileMoreParams): Promise<Api.InputFile | Api.InputFileBig | undefined>;
export {};
