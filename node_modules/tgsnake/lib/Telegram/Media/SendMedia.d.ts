import { Api } from 'telegram';
import { Snake } from '../../client';
import { TypeReplyMarkup } from '../../Utils/ReplyMarkup';
import { Entities } from '@tgsnake/parser';
export interface sendMediaMoreParams {
    silent?: boolean;
    background?: boolean;
    parseMode?: string;
    clearDraft?: boolean;
    replyToMsgId?: number;
    replyMarkup?: TypeReplyMarkup;
    entities?: Entities[];
    scheduleDate?: number;
    caption?: string;
    workers?: number;
    mimeType?: string;
    fileName?: string;
    forceDocument?: boolean;
}
/**
 * Sending message media.
 * @param snakeClient - Client
 * @param {number|string|bigint} chatId - Chat/Groups/Channel id.
 * @param {Object} media - Message Media.
 * @param more - more parameters to use.
 */
export declare function SendMedia(snakeClient: Snake, chatId: number | string | bigint, media: Api.TypeInputMedia, more?: sendMediaMoreParams): Promise<Api.TypeUpdates>;
