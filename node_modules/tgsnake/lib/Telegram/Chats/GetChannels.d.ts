import { Api } from 'telegram';
import { Snake } from '../../client';
/**
 * Get info about channels/supergroups.
 * @param snakeClient - Client
 * @param {Array} chatId - List of channel ids.
 * ```ts
 * bot.command("getChannels",async (ctx) => {
 *     let results = await ctx.telegram.getChannels([ctx.chat.id])
 *     console.log(results)
 * })
 * ```
 */
export declare function GetChannels(snakeClient: Snake, chatId: number[] | string[] | bigint[]): Promise<Api.messages.TypeChats>;
