import { Snake } from '../../client';
import { TypeReplyMarkup } from '../../Utils/ReplyMarkup';
import { Entities } from '@tgsnake/parser';
import * as Update from '../../Update';
export interface sendMessageMoreParams {
    noWebpage?: boolean;
    silent?: boolean;
    background?: boolean;
    parseMode?: string;
    clearDraft?: boolean;
    replyToMsgId?: number;
    replyMarkup?: TypeReplyMarkup;
    entities?: Entities[];
    scheduleDate?: number;
}
export declare function sendMessage(snakeClient: Snake, chatId: number | string | bigint, text: string, more?: sendMessageMoreParams): Promise<Update.UpdateShortSentMessage | Update.UpdateNewMessage | Update.UpdateNewChannelMessage | undefined>;
