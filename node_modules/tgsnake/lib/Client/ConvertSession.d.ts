import { StringSession, StoreSession } from 'telegram/sessions';
export declare function ConvertString(session: string, sessionName: string): Promise<StringSession | StoreSession>;
export declare function ConvertStore(sessionName: string): Promise<StringSession>;
