import { Api } from 'telegram';
import { Chat } from './Chat';
import { From } from './From';
import { Snake } from '../client';
export declare class ForwardMessage {
    imported?: boolean;
    from: From;
    fromName?: string;
    date: number | Date;
    channelPost?: number;
    postAuthor?: string;
    savedFromChat?: Chat;
    savedFromMsgId?: number;
    psaType?: string;
    constructor();
    init(forwardHeader: Api.MessageFwdHeader, snakeClient: Snake): Promise<this>;
}
