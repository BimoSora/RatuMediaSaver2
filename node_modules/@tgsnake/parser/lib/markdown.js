"use strict";
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.parse = void 0;
var html_1 = require("./html");
function splice(source, start, delCount, newSubStr) {
    return (source.slice(0, start) +
        newSubStr +
        source.slice(start + Math.abs(delCount)));
}
var DEFAULT_DELIMITERS = {
    "**": "b",
    __: "i",
    "```": "pre",
    "`": "code",
    "~~": "s",
    "||": "spoiler",
    "--": "u",
};
// [text](link)
//const LINK_FORMAT = "[%s](%s)"
var LINK_REGEX = /\[(.+?)\]\((.+?)\)/gi;
var LINK_ESC_REGEX = /\[(.+?)\]\\\((.+?)\)/gi;
function inRange(x, min, max) {
    return (x - min) * (x - max) <= 0;
}
function execAll(text, regex) {
    var list = [];
    var ex;
    while ((ex = regex.exec(text))) {
        list.push(ex);
    }
    return list;
}
function replaceTag(text) {
    return text
        .replace(/\&/gm, "&amp;")
        .replace(/\</gm, "&lt;")
        .replace(/\>/gm, "&rt;");
}
/**
 * parse markdown message to valid entities array.
 * you can escape the markdown format using backslash (\) <br/>
 * Valid Escape : <br/>
 * [text]\\(link) <br/>
 * \\` \\** \\``` \\|| \\-- \\~~ \\__ <br/>
 * @param {String} text - input markdown text.
 */
function parse(text) {
    var e_1, _a, e_2, _b;
    if (text == "")
        return [text, []];
    text = replaceTag(text);
    var delims = [];
    // getting all delims
    for (var key in DEFAULT_DELIMITERS) {
        delims.push(key);
    }
    var i = 0;
    var xe = new Set();
    // check if delims is escape or not.
    var igr = function (index) {
        return index > -1 && text[index - 1] == "\\" && text[index] !== "\\";
    };
    try {
        // convert the LINK_FORMAT to entities
        for (var _c = __values(execAll(text, LINK_REGEX)), _d = _c.next(); !_d.done; _d = _c.next()) {
            var match = _d.value;
            var _e = __read(match, 3), full = _e[0], text_url = _e[1], url = _e[2];
            if (text_url) {
                text =
                    text.substring(-1, Number(match.index)) +
                        text
                            .substring(Number(match.index))
                            .replace(full, "<a href=\"".concat(url, "\">").concat(text_url, "</a>"));
            }
        }
    }
    catch (e_1_1) { e_1 = { error: e_1_1 }; }
    finally {
        try {
            if (_d && !_d.done && (_a = _c.return)) _a.call(_c);
        }
        finally { if (e_1) throw e_1.error; }
    }
    try {
        // convert the LINK_ESC_REGEX to LINK_REGEX
        for (var _f = __values(execAll(text, LINK_ESC_REGEX)), _g = _f.next(); !_g.done; _g = _f.next()) {
            var match = _g.value;
            var _h = __read(match, 3), full = _h[0], text_url = _h[1], url = _h[2];
            text =
                text.substring(-1, Number(match.index)) +
                    text
                        .substring(Number(match.index))
                        .replace(full, "[".concat(text_url, "](").concat(url, ")"));
        }
    }
    catch (e_2_1) { e_2 = { error: e_2_1 }; }
    finally {
        try {
            if (_g && !_g.done && (_b = _f.return)) _b.call(_f);
        }
        finally { if (e_2) throw e_2.error; }
    }
    while (i < text.length) {
        var index = -1;
        var delim = "";
        for (var de in DEFAULT_DELIMITERS) {
            var dei = text.indexOf(de, i);
            if (dei > -1 && (index == -1 || dei < index)) {
                index = dei;
                delim = de;
            }
        }
        if (index == -1 || delim == "" || delim == undefined)
            break;
        if (!igr(index)) {
            if (xe.has(delim)) {
                xe.delete(delim);
                if (DEFAULT_DELIMITERS[delim] == "pre") {
                    text =
                        text.substring(-1, index) +
                            text
                                .substring(index)
                                .replace(delim, "</code></".concat(DEFAULT_DELIMITERS[delim], ">"));
                }
                else {
                    text =
                        text.substring(-1, index) +
                            text
                                .substring(index)
                                .replace(delim, "</".concat(DEFAULT_DELIMITERS[delim], ">"));
                }
            }
            else {
                xe.add(delim);
                if (DEFAULT_DELIMITERS[delim] == "pre") {
                    text =
                        text.substring(-1, index) +
                            text
                                .substring(index)
                                .replace(delim, "<".concat(DEFAULT_DELIMITERS[delim], "><code>"));
                }
                else {
                    text =
                        text.substring(-1, index) +
                            text
                                .substring(index)
                                .replace(delim, "<".concat(DEFAULT_DELIMITERS[delim], ">"));
                }
            }
        }
        else {
            xe.delete(delim);
            delim = "";
            text =
                text.substring(-1, index - 1) +
                    text.substring(index - 1).replace("\\", ""); //text.replace("\\","")
            index++;
        }
        i = index;
    }
    return (0, html_1.parse)(text);
}
exports.parse = parse;
